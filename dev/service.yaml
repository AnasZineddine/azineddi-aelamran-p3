apiVersion: v1
kind: Service
metadata:
  labels:
    app: argocd-app
  name: argocd-service
spec:
  type: ClusterIP
  selector:
    app: argocd-app
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
