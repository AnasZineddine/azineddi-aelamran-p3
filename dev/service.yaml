apiVersion: v1
kind: Service
metadata:
  labels:
    app: argocd
  name: argocd-service
spec:
  type: ClusterIP
  selector:
    app: argocd
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
